# 数据驱动智能编排系统项目介绍

## 一、项目概述

### 1.1 项目背景

随着云计算和边缘计算的快速发展，网络功能虚拟化（NFV）已成为现代网络架构的核心技术。在NFV环境中，虚拟网络功能（VNF）需要在物理基础设施上进行部署和调度，这一过程称为网络编排。然而，传统的静态编排方案无法适应动态变化的网络环境和QoS（服务质量）需求。

本项目设计并实现了一个**数据驱动的智能编排系统**，通过深度学习算法自动感知业务需求、智能决策VNF部署方案，实现网络资源的动态优化配置。

### 1.2 项目目标

1. **业务感知**：通过多模态数据（RGB图像 + 点云）识别业务类型和QoS需求
2. **智能编排**：基于强化学习算法实现VNF的自动部署和链路映射
3. **可视化展示**：提供前端界面实时展示物理资源和编排结果
4. **微服务架构**：采用分布式微服务设计，支持灵活扩展

---

## 二、系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              前端展示层                                  │
│    ┌─────────┐  ┌──────────┐  ┌──────────┐  ┌────────────────────┐   │
│    │  登录   │  │  注册    │  │  主界面   │  │  可视化编排结果      │   │
│    └─────────┘  └──────────┘  └──────────┘  └────────────────────┘   │
└────────────────────────────────┬────────────────────────────────────┘
                                 │
         ┌───────────────────────┼───────────────────────┐
         │                       │                       │
         ▼                       ▼                       ▼
┌─────────────────┐   ┌───────────────────┐   ┌─────────────────────────┐
│  用户中心服务   │   │    API 网关       │   │    资源管理服务         │
│   (端口 8003)   │   │   (端口 8001)     │   │    (8001端口)           │
│                 │   │                   │   │                         │
│ - 用户登录      │   │ - 编排请求入口    │   │ - 物理节点资源查询      │
│ - 用户注册      │   │ - 请求路由       │   │ - 链路资源查询          │
│ - 静态文件服务  │   │ - 结果轮询       │   │ - 资源添加/删除        │
└────────┬────────┘   └────────┬────────┘   └────────────┬────────────┘
         │                     │                          │
         │              ┌───────┴───────┐                 │
         │              │               │                 │
         ▼              ▼               ▼                 ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                           消息队列层 (Redis)                            │
│   ┌─────────────┐   ┌─────────────┐   ┌─────────────┐   ┌─────────┐   │
│   │  感知队列    │   │  编排队列    │   │  资源操作队列│   │ 结果队列 │   │
│   │rq_perception│   │rq_orchest.. │   │rq_resource..│   │rq_results│   │
│   └─────────────┘   └─────────────┘   └─────────────┘   └─────────┘   │
└──────────────────────────────┬────────────────────────────────────────┘
                               │
      ┌────────────────────────┼────────────────────────┐
      │                        │                        │
      ▼                        ▼                        ▼
┌─────────────────┐   ┌───────────────────┐   ┌─────────────────────────┐
│   业务感知服务   │   │  智能编排服务      │   │     其他微服务          │
│                 │   │                   │   │   (待扩展)              │
│ - 多模态数据    │   │ - PPO强化学习      │   │                         │
│   处理          │   │ - VNF部署决策     │   │                         │
│ - 异常检测      │   │ - 链路映射         │   │                         │
│ - QoS翻译       │   │ - 奖励计算        │   │                         │
└────────┬────────┘   └────────┬────────┘   └─────────────────────────┘
         │                    │
         │              ┌──────┴──────┐
         │              │             │
         ▼              ▼             ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                             数据存储层 (MySQL)                           │
│  ┌──────────────┐  ┌──────────────────┐  ┌──────────────────────────┐  │
│  │ qos_user     │  │ business_        │  │ intelligent_             │  │
│  │ _center      │  │ awareness        │  │ _orchestration          │  │
│  │              │  │                  │  │                          │  │
│  │ - 用户信息    │  │ - 工业数据       │  │ - 物理资源清单           │  │
│  │ - 角色权限    │  │ - 感知日志       │  │ - 编排日志              │  │
│  │              │  │ - 映射规则       │  │ - 告警记录              │  │
│  └──────────────┘  └──────────────────┘  └──────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2 技术栈

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| 前端 | HTML5 + JavaScript (ES6) | 响应式布局，Canvas绘图 |
| 后端 | Python 3.9+ | 微服务架构 |
| Web框架 | FastAPI (网关) + HTTP Server (用户中心) | 高性能API服务 |
| 消息队列 | Redis | 异步通信，任务调度 |
| 数据库 | MySQL 8.0 | 持久化存储 |
| 深度学习 | PyTorch + Stable-Baselines3 | 模型训练与推理 |
| 强化学习 | PPO (Proximal Policy Optimization) | 智能决策算法 |

### 2.3 服务部署

| 服务名称 | 端口 | 功能描述 |
|----------|------|----------|
| 用户中心 | 8003 | 用户登录注册，静态文件服务 |
| API网关 | 8001 | 请求入口，编排流水线调度 |
| 感知服务 | 后台运行 | 业务感知，QoS翻译 |
| 编排服务 | 后台运行 | PPO决策，资源分配 |

---

## 三、核心功能模块

### 3.1 用户管理模块

**功能描述**：提供用户注册、登录、权限管理功能

- **注册**：用户填写邮箱、密码进行注册
- **登录**：验证用户名密码，返回Token
- **权限**：支持系统管理员和普通用户角色

**数据库表**：`qos_user_center.t_user`

### 3.2 物理资源管理模块

**功能描述**：管理物理节点和链路的增删改查

**物理节点属性**：
- 节点ID、名称
- CPU负载率、内存负载率
- vCPU核心数、内存大小、存储容量
- 带宽、能耗系数

**物理链路属性**：
- 链路ID
- 源节点、目标节点
- 带宽、时延
- 路径ID（用于分组）

**前端可视化**：
- Canvas绘制节点（圆形）和链路（连线）
- 节点显示CPU/内存信息
- 链路显示带宽信息

### 3.3 业务感知模块

**功能描述**：通过多模态数据识别业务类型，转换为QoS需求

**输入**：
- RGB图像路径
- 点云文件（.tiff格式）路径

**处理流程**：
1. 加载预训练的3D点云分类模型（PointNet++）
2. 对点云数据进行特征提取和分类
3. 根据分类结果确定业务异常程度
4. 翻译为QoS向量：[带宽, 时延, 优先级, 丢包率]

**输出**：
- qos_vector：4维QoS需求向量
- anomaly_score：异常分数（0-1）
- risk_level：风险等级（NORMAL/MEDIUM/HIGH/CRITICAL）

### 3.4 智能编排模块

**功能描述**：基于强化学习算法自动完成VNF部署和链路映射

**输入**：
- QoS向量（来自感知服务）
- 物理资源状态（来自数据库）

**输出**：
- vnf_node：VNF到物理节点的映射 [node1, node2, ...]
- link_path：虚拟链路到物理路径的映射 [path1, path2, ...]
- reward：奖励值（正值表示方案可行）
- qos_ok：QoS是否满足
- resource_ok：资源是否充足

---

## 四、算法实现

### 4.1 强化学习算法 - PPO

**算法原理**：

PPO（Proximal Policy Optimization）是一种基于策略梯度的强化学习算法，其核心思想是在保证策略更新的同时避免策略变化过大。

**策略网络结构**：
- 输入：物理节点特征 + 链路特征 + VNR特征
- 输出：离散动作（节点选择 + 路径选择）

**动作空间设计**：
- 节点动作：5个物理节点（0-4）
- 路径动作：4条候选路径（0-3）

**动作空间维度**：[5, 5, 4, 4]（2个VNF + 2条链路）

### 4.2 奖励函数设计

奖励函数是多智能体学习的关键，本系统设计了复合奖励函数：

```
Total_Reward = QoS奖励 + 分布奖励 + 能耗惩罚 + 负载均衡奖励
```

**各组成部分**：

1. **QoS奖励**
   ```
   lat_score = alpha + (req_lat - actual_lat) * 50  (满足时)
             = (req_lat - actual_lat) * 200         (不满足时)

   bw_score = (actual_bw - req_bw) * 40             (满足时)
            = (actual_bw - req_bw) * 300             (不满足时)

   qos_total = (lat_score + bw_score) * (1 + priority)
   ```

2. **分布奖励**
   - VNF部署在不同节点：+50分
   - VNF部署在同一节点：-100分

3. **能耗惩罚**
   ```
   energy_penalty = -Σ(节点能耗) * gamma
   ```

4. **负载均衡奖励**
   ```
   load_balance = -平均节点负载 * 20
   ```

### 4.3 环境模拟器

**GraphVNEEnv** 是基于Gymnasium构建的强化学习环境：

**状态空间（Observation）**：
- 节点特征：CPU、内存、能耗、度数（4维 × 5节点 = 20维）
- 链路特征：带宽、归一化时延（2维 × 8链路 = 16维）
- VNR特征：QoS向量 + VNF数量比（5维）
- 总维度：41维

**动作空间（Action）**：
- MultiDiscrete([5, 5, 4, 4])

**约束检查**：
1. 资源检查：节点CPU是否满足VNF需求
2. 连通性检查：使用BFS验证路径连通性
3. QoS检查：带宽、时延、丢包率是否满足

### 4.4 训练策略

**预热训练**：
- 训练步数：10000步
- 学习率：5e-4
- 批量大小：64
- 折扣因子：0.99

**探索策略**：
- 使用确定性策略（deterministic=True）
- 动作裁剪：将超出范围的动作映射到有效范围
- 随机化初始资源分布，增加训练多样性

---

## 五、数据流程

### 5.1 完整编排流程

```
1. 用户提交    前端 → 网关：POST /pipeline
                     │
2. 数据入库    网关 → MySQL：写入t_industrial_data
                     │
3. 消息队列    网关 → Redis：发布到rq_perception
                     │
4. 业务感知    感知服务消费消息 → 模型推理 → QoS翻译
                     │
5. 编排触发    感知服务 → Redis：发布到rq_orchestration
                     │
6. 智能决策    编排服务消费消息 → PPO决策 → 奖励计算
                     │
7. 结果存储    编排服务 → MySQL：写入t_orchestration_log
                     │
8. 结果返回    编排服务 → Redis：发布到rq_results
                     │
9. 结果轮询    网关 → Redis：轮询获取结果
                     │
10. 前端展示   网关 → 前端：返回编排结果
```

### 5.2 数据格式

**编排请求**：
```json
{
  "businessImagePath": "/data/sample/001.png",
  "pointCloudPath": "/data/sample/001.tiff"
}
```

**编排响应**：
```json
{
  "status": "success",
  "task_id": "123",
  "vnf_node": [0, 3],
  "link_path": [1],
  "reward": 85.5,
  "qos_ok": true,
  "resource_ok": true,
  "qos_vector": [10.0, 5.0, 1.0, 0.01]
}
```

---

## 六、前端展示

### 6.1 页面结构

1. **登录页** (`index.html`)
   - 邮箱输入、密码输入
   - 登录按钮、注册链接

2. **注册页** (`register.html`)
   - 邮箱、密码、确认密码
   - 注册按钮

3. **工作台** (`sandbox/index.html`)
   - 路径输入（RGB图像 + 点云）
   - 资源选择/刷新
   - 添加物理节点/链路
   - 删除物理资源
   - 物理资源可视化
   - 编排结果 + 可视化

### 6.2 可视化功能

**物理资源可视化**：
- 节点：蓝色圆形，标注名称、CPU、内存
- 链路：灰色连线，中点标注带宽
- 布局：圆形布局

**编排结果可视化**：
- 选中节点：根据qos_ok/resource_ok显示颜色
  - 绿色：全部满足
  - 红色：任一不满足
- 选中链路：同节点颜色逻辑

---

## 七、数据库设计

### 7.1 数据库列表

| 数据库名 | 用途 |
|----------|------|
| qos_user_center | 用户中心 |
| business_awareness | 业务感知数据 |
| intelligent_orchestration | 智能编排数据 |

### 7.2 核心表结构

**t_user（用户表）**
- user_id, username, password_hash, role

**t_industrial_data（工业数据表）**
- data_id, rgb_path, pcd_path, create_time

**t_resource_inventory（资源清单表）**
- resource_id, resource_type, current_state, is_active

**t_orchestration_log（编排日志表）**
- log_id, data_id, decision_plan, reward, create_time

---

## 八、系统特点与创新点

### 8.1 主要特点

1. **数据驱动**：通过多模态数据感知业务需求，而非静态配置
2. **智能决策**：采用深度强化学习自动学习最优部署策略
3. **可视化**：前端实时展示物理资源和编排结果
4. **微服务**：松耦合架构，易于扩展和维护

### 8.2 创新点

1. **多模态感知**：结合RGB图像和3D点云进行业务识别
2. **复合奖励函数**：综合考虑QoS、分布、能耗、负载均衡
3. **动作裁剪机制**：确保模型输出在物理可行范围内
4. **连通性验证**：BFS算法保证路径有效性

---

## 九、使用说明

### 9.1 启动服务

```bash
# 1. 初始化数据库
mysql -h 127.0.0.1 -u root -p < docker/mysql/init.sql

# 2. 启动后端服务
python backend_server.py        # 端口8003
python -m services.gateway       # 端口8001
python -m services.perception_service
python -m services.orchestration_service
```

### 9.2 访问地址

- 前端：http://localhost:8003
- API：http://localhost:8001

---

## 十、总结与展望

本项目实现了一个完整的数据驱动智能编排系统，涵盖了从前端界面到后端算法的大部分功能。通过PPO强化学习算法，实现了VNF的智能部署和链路映射。

**后续优化方向**：
1. 增加更多业务场景支持
2. 优化PPO模型训练效果
3. 添加更多微服务（如基础设施服务）
4. 支持Kubernetes容器编排

---

*文档生成时间：2026-04-05*